- scans appear based on organization, and is only allowed to admins of that organization
- enforce meals rules, and badge times rules
- add statistics to the page, like number of badges so far
- add api key validation into the public api